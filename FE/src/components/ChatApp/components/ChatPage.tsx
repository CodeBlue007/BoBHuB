import styled from 'styled-components';
import { useState } from 'react';
import { Paper } from '@mui/material';

const ChatPage = () => {
  return (
    <Paper sx={{ width: '300px', height: '500px', backgroundColor: 'crimson', zIndex:10 }} elevation={15}>
      ChatPage
    </Paper>
  );
};

export default ChatPage;
